spring.application.name=RemoteTrack

# Server port (for Render deployment)
server.port=${PORT:8080}

# Datasource (Supabase PostgreSQL)
spring.datasource.url=${DATABASE_URL:jdbc:postgresql://db.tuxwzcsvxszfnonibmor.supabase.co:5432/postgres}
spring.datasource.username=${DB_USERNAME:postgres}
spring.datasource.password=${DB_PASSWORD:pankaj@123PS}

# JPA / Hibernate
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=true